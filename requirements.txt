# --- Kaggle pre-installed (available on Kaggle without installing) ---
# numpy, pandas, scikit-learn, scipy, matplotlib, seaborn, plotly,
# torch, transformers, datasets, tqdm, pillow, geopandas, requests

# --- Project dependencies (install these locally) ---

# Data & visualization (install locally to match Kaggle environment)
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.22.0
statsmodels>=0.14.0
